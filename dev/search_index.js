var documenterSearchIndex = {"docs":
[{"category":"section","location":"guide/#Guide","page":"Guide","text":"This page walks through the whole workflow. Everything here runs on the CPU; see GPU (CUDA) for the device path and Differentiable edges for gradients.","title":"Guide"},{"category":"section","location":"guide/#The-four-step-workflow","page":"Guide","text":"Every Octopus program has the same shape:\n\nCreate a handle with TNS and set the radius with set_search_radius!.\nRegister coordinates with add_point_set!.\nDeclare which set queries which with set_active_search!.\nBuild with run!, then query.\n\nusing Octopus\n\nxyz = rand(Float32, 3, 100_000)\n\ntns = TNS(Float32; ndims = 3)     # 1. handle\nset_search_radius!(tns, 0.02f0)\nid = add_point_set!(tns, xyz)     # 2. point set\nset_active_search!(tns, id, id)   # 3. self-search\nrun!(tns)                          # 4. build","title":"The four-step workflow"},{"category":"section","location":"guide/#Coordinate-layout","page":"Guide","text":"Coordinates are (NDIMS, N) matrices — one column per point, so each point's components are contiguous. TNS(Float32; ndims = 2) builds a quadtree over (2, N) input; the default ndims = 3 builds an octree over (3, N).\n\nThe element type of the coordinates must match the TNS element type. TNS{Float32} is the tested and benchmarked configuration; TNS{Float64} works but is unbenchmarked.\n\nnote: Coordinates are referenced, not copied\nadd_point_set! keeps a reference to your matrix. Mutating it in place and calling run! again is the intended update path for a simulation loop — see Updating points between steps.","title":"Coordinate layout"},{"category":"section","location":"guide/#Point-sets-and-active-pairs","page":"Guide","text":"A TNS can hold several point sets. add_point_set! returns an integer id, and an active pair (qid, tid) declares that points from set qid will look for neighbors in set tid. Pairs are directed, so a mutual search between two sets needs both directions registered:\n\nfluid = add_point_set!(tns, fluid_xyz)\nwalls = add_point_set!(tns, wall_xyz)\n\nset_active_search!(tns, fluid, fluid)   # fluid ↔ fluid\nset_active_search!(tns, fluid, walls)   # fluid → walls\nset_active_search!(tns, walls, fluid)   # walls → fluid\nrun!(tns)\n\nRegistering a pair twice is a no-op: the existing buffers are kept and refilled by the next run!. All point sets on one handle must live on the same backend — you cannot mix host and device arrays.","title":"Point sets and active pairs"},{"category":"section","location":"guide/#Iterating-neighbors","page":"Guide","text":"for_each_neighbor is the primitive. It visits neighbors during tree traversal instead of collecting them, so the hot loop allocates nothing:\n\nfor i in 1:size(xyz, 2)\n    for_each_neighbor(tns, id, id, i) do j\n        # j is an Int32 index into the target set\n    end\nend\n\nwarning: The self-pair is not filtered\nWhen qid == tid, the callback also fires for j == i. This matches the paper's contract. Skip it yourself if your kernel needs to:i32 = Int32(i)\nfor_each_neighbor(tns, id, id, i) do j\n    j == i32 && return\n    # ...\nend\n\nThe traversal stack is per-thread, so calling for_each_neighbor from inside Threads.@threads or a Polyester.@batch loop is safe.","title":"Iterating neighbors"},{"category":"section","location":"guide/#Collecting-a-list-instead","page":"Guide","text":"get_neighborlist allocates a fresh Vector{Int32} per call and does exclude the self-pair. Convenient for tests and exploration, wasteful in a hot loop:\n\nneighbors = get_neighborlist(tns, id, id, 1)","title":"Collecting a list instead"},{"category":"section","location":"guide/#Materializing-all-lists-at-once","page":"Guide","text":"materialize_all_neighbors! fills CSR-style buffers for every active pair in one pass. This is opt-in — the default iteration path stores nothing. Read the result off the handle:\n\nmaterialize_all_neighbors!(tns)\n\npair_idx = findfirst(==((Int32(id), Int32(id))), tns.active_pairs)\nbuf = tns.neighbor_buffers[pair_idx]\n\n# offsets are 0-based; neighbors of point i are:\nlo = buf.offsets[i] + 1\nhi = buf.offsets[i + 1]\njs = buf.flat[lo:hi]\n\nMaterializing costs memory — roughly 21 bytes per particle at steady state versus 15 for the lazy path in the package's own benchmark — so reach for it only when a downstream consumer genuinely needs a dense list. It is CPU-only; on GPU use build_edges or in-kernel iteration.","title":"Materializing all lists at once"},{"category":"section","location":"guide/#Edge-buffers-for-graph-models","page":"Guide","text":"build_edges produces a flat-COO edge list — the shape a message-passing GNN wants — and returns it as a NamedTuple:\n\ne = build_edges(tns, id, id)\n\ne.senders           # Vector{Int32}, the target index j\ne.receivers         # Vector{Int32}, the query index i\ne.rel_displacement  # (NDIMS, n_edges), (q[:,i] - t[:,j]) / radius\ne.rel_dist_norm     # (1, n_edges),     ‖q[:,i] - t[:,j]‖ / radius\n\nBoth feature arrays are normalized by the search radius, so they land in [-1, 1] and [0, 1] respectively without further scaling. Unlike for_each_neighbor, a self-pair search does exclude j == i here.\n\nbuild_edges! is the same operation returning the underlying EdgeBuffer. Either way the storage is reused across calls: a second build_edges on the same pair overwrites the arrays in place. Copy them if you need the values to outlive the next call.","title":"Edge buffers for graph models"},{"category":"section","location":"guide/#Updating-points-between-steps","page":"Guide","text":"For a simulation loop, mutate the coordinate matrix you already registered and rebuild:\n\nfor step in 1:n_steps\n    integrate!(xyz)        # your update, in place\n    run!(tns)              # rebuild against the new positions\n    # ... query ...\nend\n\nIf you need to swap in a different matrix, the two entry points differ in strictness:\n\nupdate_point_set! requires the same N and throws DimensionMismatch otherwise — the safer choice when the count is meant to be fixed.\nresize_point_set! accepts any N.\n\nBoth mark the set dirty; run! only rebuilds dirty sets, so untouched sets carry their tree over.\n\nwarning: Rebuilding invalidates materialized data\nrun! clears the materialized flags on all neighbor and edge buffers. Anything you read out of them must be refilled — or copied — after each rebuild.","title":"Updating points between steps"},{"category":"section","location":"guide/#Refit-mode","page":"Guide","text":"For time-stepping workloads where points move much less than one cell per step, set_refit_mode! enables the paper's almost-sorted path:\n\nset_refit_mode!(tns, true)\n\nWith refit mode on, the Morton codes from the previous build are retained so the next run! can detect cell crossings instead of re-deriving the ordering from scratch. With it off (the default) that scratch is released after every run!, which keeps steady-state memory lower. Turn it on when you rebuild every step and positions change slowly; leave it off for one-shot queries or when points jump arbitrarily.","title":"Refit mode"},{"category":"section","location":"guide/#Z-sorting-user-data","page":"Guide","text":"The build computes a Morton-order permutation. Reordering your per-point data to match it improves cache locality for everything downstream. apply_zsort! returns a permuted copy of any array whose last axis is the point axis — a length-N vector or an (F, N) matrix:\n\nrun!(tns)\nprepare_zsort!(tns)               # asserts the permutation is available\n\nmasses    = apply_zsort!(tns, id, masses)\nvelocities = apply_zsort!(tns, id, velocities)   # (3, N) works too\n\nprepare_zsort! is a precondition check that mirrors the C++ API — it gives a clear error if run! has not been called yet. It allocates nothing and is safe (but unnecessary) to call every step. Note that apply_zsort! returns a new array rather than permuting in place, despite the !.","title":"Z-sorting user data"},{"category":"section","location":"guide/#Which-API-is-which?","page":"Guide","text":"These names mirror the paper and the C++ TreeNSearch reference implementation: TNS, set_search_radius!, add_point_set!, resize_point_set!, set_active_search!, run!, for_each_neighbor, get_neighborlist, prepare_zsort!, apply_zsort!.\n\nThese are Julia-only additions layered on top, for Julia idioms or GPU usage: set_refit_mode!, update_point_set!, materialize_all_neighbors!, build_edges, build_edges!, EdgeBuffer, build_edges_diff, device_view, for_each_neighbor_device, @for_each_neighbor_device_inline and its _2d variant.\n\n\n\nCopyright (c) 2026 Josef Jouaux, Chair of Mechatronics, University of Augsburg. Released under the MIT License.","title":"Which API is which?"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"Every exported symbol, grouped by role. See the Guide for how they fit together.\n\n","title":"API reference"},{"category":"section","location":"api/#Handle-and-configuration","page":"API reference","text":"","title":"Handle and configuration"},{"category":"section","location":"api/#Point-sets","page":"API reference","text":"","title":"Point sets"},{"category":"section","location":"api/#Search-registration-and-build","page":"API reference","text":"","title":"Search registration and build"},{"category":"section","location":"api/#Neighbor-iteration-(host)","page":"API reference","text":"","title":"Neighbor iteration (host)"},{"category":"section","location":"api/#Neighbor-iteration-(device)","page":"API reference","text":"These require the CUDA extension — see GPU (CUDA).","title":"Neighbor iteration (device)"},{"category":"section","location":"api/#Edge-construction","page":"API reference","text":"","title":"Edge construction"},{"category":"section","location":"api/#Z-sorting","page":"API reference","text":"\n\nCopyright (c) 2026 Josef Jouaux, Chair of Mechatronics, University of Augsburg. Released under the MIT License.","title":"Z-sorting"},{"category":"type","location":"api/#Octopus.TNS","page":"API reference","text":"TNS(T = Float32; ndims = 3) -> TNS{T,ndims}\n\nThe main handle: holds the registered point sets, their trees, the active search pairs and all reusable scratch. T is the coordinate element type and ndims selects the spatial dimension — 3 builds an octree, 2 a quadtree.\n\nTNS{Float32} is the tested and benchmarked configuration. TNS{Float64} works but is unbenchmarked.\n\nThe backend is not chosen here. It is inferred on the first add_point_set! from the array type of the coordinates: a plain Array selects the CPU path, a GPU array (e.g. CuArray) the CUDA path. All point sets on one handle must share a backend.\n\nA typical setup, in order:\n\ntns = TNS(Float32; ndims = 3)\nset_search_radius!(tns, 0.02f0)\nid = add_point_set!(tns, xyz)      # xyz is (3, N)\nset_active_search!(tns, id, id)\nrun!(tns)\n\nPer-thread traversal stacks are preallocated for Threads.maxthreadid() threads and grown on demand, so queries are safe to issue from inside threaded loops.\n\n\n\n\n\n","title":"Octopus.TNS"},{"category":"function","location":"api/#Octopus.set_search_radius!","page":"API reference","text":"set_search_radius!(tns, r) -> tns\n\nSet the fixed search radius r for every active pair. Must be called before run!. Changing the radius marks all point sets dirty, so the next run! rebuilds their trees.\n\nThrows ArgumentError if r <= 0.\n\n\n\n\n\n","title":"Octopus.set_search_radius!"},{"category":"function","location":"api/#Octopus.set_refit_mode!","page":"API reference","text":"set_refit_mode!(tns, on::Bool) -> tns\n\nToggle the paper's almost-sorted refit path, intended for time-stepping simulations where points move little between steps. When on, the Morton codes from the previous build are retained so the next run! can detect cell crossings instead of rebuilding from scratch; when off (the default) that scratch is released after each run! to keep steady-state memory minimal.\n\nMarks all point sets dirty. Julia-only convenience: the upstream C++ TreeNSearch API does not expose this as a user toggle.\n\n\n\n\n\n","title":"Octopus.set_refit_mode!"},{"category":"function","location":"api/#Octopus.add_point_set!","page":"API reference","text":"add_point_set!(tns, coords) -> set_id\n\nRegister an (NDIMS, N) coordinate matrix as a new point set and return its integer id, used to refer to the set in set_active_search!, for_each_neighbor, build_edges and friends.\n\ncoords is referenced, never copied — mutating it in place and calling run! again is the intended update path for simulations.\n\nThe backend is inferred from the array type on the first call: a plain Array selects :cpu, a GPU array (e.g. CuArray) selects :cuda. All point sets registered on one TNS must share a backend, and GPU coords require CUDA.jl to be loaded so the extension is active.\n\nThrows DimensionMismatch if size(coords, 1) != NDIMS.\n\n\n\n\n\n","title":"Octopus.add_point_set!"},{"category":"function","location":"api/#Octopus.resize_point_set!","page":"API reference","text":"resize_point_set!(tns, set_id, coords) -> tns\n\nReplace the coordinates of point set set_id, allowing the point count N to change. Marks the set dirty so the next run! rebuilds its tree.\n\nUse update_point_set! when N is expected to stay the same and you want that checked.\n\nThrows DimensionMismatch if size(coords, 1) != NDIMS.\n\n\n\n\n\n","title":"Octopus.resize_point_set!"},{"category":"function","location":"api/#Octopus.update_point_set!","page":"API reference","text":"update_point_set!(tns, set_id, coords) -> tns\n\nJulia-only convenience over resize_point_set!: refresh the coordinates of a point set under a strict same-N precondition. Throws DimensionMismatch if N changes — callers who genuinely need a different N should use resize_point_set! instead. Not part of the upstream C++ TreeNSearch API.\n\n\n\n\n\n","title":"Octopus.update_point_set!"},{"category":"function","location":"api/#Octopus.set_active_search!","page":"API reference","text":"set_active_search!(tns, qid, tid)\n\nRegister the directed pair (query=qid, target=tid) as an active search. A fresh neighbor buffer and edge buffer are attached on first registration. Calling with an already-registered pair is a no-op — the existing buffers are kept and will be refilled by the next run! / materialize_all_neighbors! / build_edges! call. There is no need (or way) to \"reset\" a pair through this function.\n\n\n\n\n\n","title":"Octopus.set_active_search!"},{"category":"function","location":"api/#Octopus.run!","page":"API reference","text":"run!(tns) -> tns\n\nBuild (or refit) the search structure for every dirty point set: bin points to Morton cells, sort, build the tree top-down, then fit node bounds bottom-up. Dispatches to the CPU or CUDA path according to the backend inferred by add_point_set!.\n\nCall this after set_search_radius! and at least one add_point_set!, and again whenever coordinates change. Neighbor and edge buffers are invalidated, so any previously materialized lists must be rebuilt before use.\n\nThrows ArgumentError if no radius was set or no point set was added.\n\n\n\n\n\n","title":"Octopus.run!"},{"category":"function","location":"api/#Octopus.for_each_neighbor","page":"API reference","text":"for_each_neighbor(f, tns, qid, tid, i)\n\nCall f(j) for every point j in target set tid lying within radius of point i in query set qid. This is the zero-allocation iteration primitive — neighbors are visited during traversal rather than collected, so nothing lands on the heap in the hot loop.\n\nIntended for do-block syntax:\n\nfor_each_neighbor(tns, qid, tid, i) do j\n    # use neighbor j\nend\n\nRun run!(tns) first. Self-pairs are not filtered: when qid == tid the callback also fires for j == i, so skip it yourself if unwanted (this is what get_neighborlist does).\n\nThread-safe — each caller gets its own traversal stack, indexed by thread id.\n\nHost-side and CPU-only; on a CUDA TNS use for_each_neighbor_device inside your own kernel, or build_edges for a materialized edge list.\n\n\n\n\n\n","title":"Octopus.for_each_neighbor"},{"category":"function","location":"api/#Octopus.get_neighborlist","page":"API reference","text":"get_neighborlist(tns, qid, tid, i) -> Vector{Int32}\n\nCollect the indices of all points in set tid that lie within radius of point i in set qid. Allocates a fresh Vector each call — use for_each_neighbor for the zero-allocation path.\n\n\n\n\n\n","title":"Octopus.get_neighborlist"},{"category":"function","location":"api/#Octopus.materialize_all_neighbors!","page":"API reference","text":"materialize_all_neighbors!(tns)\n\nBuild CSR-style neighbor lists for every active pair. Opt-in; default for_each_neighbor iteration uses no materialized storage.\n\nCPU-only in v0.1. On a CUDA TNS, use for_each_neighbor_device / @for_each_neighbor_device_inline for in-kernel iteration, or build_edges for a flat-COO edge list.\n\n\n\n\n\n","title":"Octopus.materialize_all_neighbors!"},{"category":"function","location":"api/#Octopus.device_view","page":"API reference","text":"device_view(tns, qid, tid) -> DeviceView\n\nReturn an isbits handle holding the device arrays for the active pair (qid, tid), safe to pass into a user @cuda kernel as an argument. Inside the kernel, iterate with for_each_neighbor_device or the @for_each_neighbor_device_inline macro.\n\nThe handle borrows the tree and coordinate arrays owned by tns, so it is invalidated by the next run!; take a fresh view after each rebuild.\n\nRequires a CUDA TNS (throws otherwise) and using CUDA so the extension is loaded. Julia-only addition, not part of the upstream C++ TreeNSearch API.\n\n\n\n\n\n","title":"Octopus.device_view"},{"category":"function","location":"api/#Octopus.for_each_neighbor_device","page":"API reference","text":"for_each_neighbor_device(f, dv::DeviceView, i)\n\nDevice-side counterpart of for_each_neighbor: call f(j) for every point j within radius of query point i, from inside a @cuda kernel. dv comes from device_view.\n\nThe callback must not write to a captured scalar — a mutable local would be boxed and rejected by the GPU compiler. Write into an array slot instead:\n\nfor_each_neighbor_device(dv, i) do j\n    @inbounds counts[i] += Int32(1)\nend\n\nUnlike the host for_each_neighbor, the self-pair filter is not applied; skip j == i yourself when qid == tid. For the hot path prefer @for_each_neighbor_device_inline, which keeps the traversal cursor in a register.\n\nDefined by the OctopusCUDAExt extension; requires using CUDA.\n\n\n\n\n\n","title":"Octopus.for_each_neighbor_device"},{"category":"macro","location":"api/#Octopus.@for_each_neighbor_device_inline","page":"API reference","text":"@for_each_neighbor_device_inline dv i j body\n\nInline expansion of the device-side neighbor traversal for use inside @cuda kernels. Faster than for_each_neighbor_device(dv, i) do j ... end when the body mutates local scalars (closure boxing forces a global-memory round-trip per emitted edge). Emits body for every neighbor j whose distance to point i is ≤ dv.radius. The user is responsible for the self-pair filter when qid == tid.\n\nRequires the CUDA extension to be loaded (so dv is a DeviceView).\n\n\n\n\n\n","title":"Octopus.@for_each_neighbor_device_inline"},{"category":"macro","location":"api/#Octopus.@for_each_neighbor_device_inline_2d","page":"API reference","text":"@for_each_neighbor_device_inline_2d dv i j body\n\n2D quadtree variant of @for_each_neighbor_device_inline. Same shape and purpose; expects dv::DeviceView{T,2} and a 4-way child fan-out. Use this inside @cuda kernels for 2D TNS{T,2} workloads when closure boxing would spill a per-edge cursor to global memory.\n\n\n\n\n\n","title":"Octopus.@for_each_neighbor_device_inline_2d"},{"category":"type","location":"api/#Octopus.EdgeBuffer","page":"API reference","text":"EdgeBuffer\n\nFlat-COO storage for the neighbor relation of one active pair, shaped for graph-neural-network consumers. Populated by build_edges!; build_edges returns the same arrays as a NamedTuple.\n\nFields, with k ranging over the n_edges emitted edges:\n\nsenders::AbstractVector{Int32} — target index j.\nreceivers::AbstractVector{Int32} — query index i.\nrel_displacement::AbstractMatrix{T} — (NDIMS, n_edges), the radius-normalized displacement (coords_q[:, i] - coords_t[:, j]) / radius.\nrel_dist_norm::AbstractMatrix{T} — (1, n_edges), the radius-normalized distance ‖coords_q[:, i] - coords_t[:, j]‖ / radius.\nn_edges::Int32, materialized::Bool — fill state.\n\nThe arrays live on the same device as the point set, so a CUDA TNS yields CuArrays ready to hand to a model. Storage is reused across calls: a later build_edges! on the same pair overwrites it. Take copies if you need the values to outlive the next call — build_edges_diff does exactly this.\n\nJulia-only addition, not part of the upstream C++ TreeNSearch API.\n\n\n\n\n\n","title":"Octopus.EdgeBuffer"},{"category":"function","location":"api/#Octopus.build_edges","page":"API reference","text":"build_edges(tns, qid, tid) -> NamedTuple\n\nConvenience wrapper that returns (senders, receivers, rel_displacement, rel_dist_norm) as a NamedTuple. Same backing storage as the underlying EdgeBuffer, so the arrays alias tns.edge_buffers[…].\n\n\n\n\n\n","title":"Octopus.build_edges"},{"category":"function","location":"api/#Octopus.build_edges!","page":"API reference","text":"build_edges!(tns, qid, tid) -> EdgeBuffer\n\nPopulate the EdgeBuffer for the active pair (qid, tid) with flat-COO edge data: senders, receivers, rel_displacement (normalized coords_q[:,i] - coords_t[:,j]) and rel_dist_norm (normalized distance). Self-pair (qid == tid) excludes j == i.\n\nRun set_active_search!(tns, qid, tid) and run!(tns) first.\n\n\n\n\n\n","title":"Octopus.build_edges!"},{"category":"function","location":"api/#Octopus.build_edges_diff","page":"API reference","text":"build_edges_diff(coords, tns, id, radius) -> NamedTuple\n\nDifferentiable wrapper around build_edges(tns, id, id). Returns a NamedTuple with (senders, receivers, rel_displacement, rel_dist_norm) whose arrays are fresh copies (so backprop closures are not affected by subsequent buffer overwrites). Gradients flow back to coords (the tree topology and the search radius are non-differentiable).\n\nThe user is responsible for keeping coords in sync with tns — typically by calling update_point_set!(tns, id, coords); run!(tns) under Zygote.@ignore before this function.\n\nLoaded via the OctopusChainRulesCoreExt and OctopusCUDAChainRulesCoreExt weak extensions; requires using ChainRulesCore (and using CUDA for the GPU path).\n\n\n\n\n\n","title":"Octopus.build_edges_diff"},{"category":"function","location":"api/#Octopus.prepare_zsort!","page":"API reference","text":"prepare_zsort!(tns) -> tns\n\nPrecondition check matching the C++ TreeNSearch API: asserts that the permutation for every registered point set is populated (i.e. run!(tns) has been called since the last build). Does not allocate or mutate state — call this when you want a clear error before invoking apply_zsort! from a hot loop. It is safe but unnecessary to call it every step; apply_zsort! itself does not depend on it.\n\n\n\n\n\n","title":"Octopus.prepare_zsort!"},{"category":"function","location":"api/#Octopus.apply_zsort!","page":"API reference","text":"apply_zsort!(tns, set_id, user_array) -> permuted_array\n\nReturn a new array whose entries follow the z-order permutation built for point set set_id. Accepts a 1-D vector (length N) or a 2-D matrix ((F, N)); higher-rank inputs are rejected. Works on both CPU and CUDA backends — the returned array lives on the same device as user_array.\n\n\n\n\n\n","title":"Octopus.apply_zsort!"},{"category":"section","location":"gpu/#GPU-usage","page":"GPU (CUDA)","text":"Octopus.jl runs on NVIDIA GPUs via the OctopusCUDAExt extension that loads when using CUDA is in scope.\n\nusing Octopus\nusing CUDA\n\nxyz = CUDA.rand(Float32, 3, 100_000)\ntns = TNS(Float32; ndims = 3)\nset_search_radius!(tns, 0.02f0)\nid = add_point_set!(tns, xyz)\nset_active_search!(tns, id, id)\nrun!(tns)","title":"GPU usage"},{"category":"section","location":"gpu/#On-device-build","page":"GPU (CUDA)","text":"run! builds the octree entirely on the GPU — the origin reduction, Morton encoding, sort, level-synchronous topology pass and bottom-up bounds refit all run in device kernels, and the coordinates never round-trip through the host. For a per-timestep simulation or training loop that rebuilds every step this keeps the build off the critical CPU path (≈2–4 ms for 5k–200k points on an A30). The resulting tree is structurally identical to the CPU build, so edge sets match the CPU backend exactly.\n\nThere are two ways to iterate neighbors from inside your own @cuda kernels:\n\nfor_each_neighbor_device(dv, i) do j ... end — closure form. Easiest to read, fine when the body is read-only or only writes to global memory slots indexed by i.\n@for_each_neighbor_device_inline dv i j body — macro form. Inlines the tree traversal directly into your kernel. Use this when the body mutates per-thread local scalars (typical for write-pass kernels with a per-edge cursor).\n\nThe two paths have identical semantics: for every neighbor j of point i whose distance is ≤ search_radius, the body executes with j bound to the neighbor index. The user is responsible for filtering the self-pair j == i if their workflow needs it (mirroring the paper's for_each_neighbor contract — the host's for_each_neighbor does not filter self either).","title":"On-device build"},{"category":"section","location":"gpu/#When-the-macro-matters","page":"GPU (CUDA)","text":"If your kernel body mutates a captured scalar (e.g. a per-thread cursor), the closure form forces Julia to box that scalar. On GPU the box lives in global memory, so every emitted edge does a load + store of the cursor. The macro form keeps the cursor in a register and writes it back once.\n\nMeasured on an NVIDIA A30 with a write-pass kernel that maintains a per-thread edge cursor:\n\nScene Closure Macro Speedup\n2D N=1k 1.62 ms 0.79 ms 2.05×\n2D N=5k 9.87 ms 5.63 ms 1.75×\n2D N=20k 63.4 ms 44.4 ms 1.43×\n\nEnd-to-end build + count + alloc + write (lower is better):\n\nScene Octopus (macro) PointNeighbors.jl Speedup\n2D dam-break N=1k 1.55 ms 14.6 ms 9.4×\n2D dam-break N=5k 9.3 ms 28.4 ms 3.1×\n2D dam-break N=20k 58 ms 73 ms 1.3×\n3D uniform N=200k 38 ms 209 ms 5.5×\n\nEdge sets are byte-for-byte identical to the closure path.","title":"When the macro matters"},{"category":"section","location":"gpu/#Closure-form-(read-only-body)","page":"GPU (CUDA)","text":"When all the body does is write to a per-point output slot (array[i]) or read-only computation, the closure form is fine:\n\nfunction count_neighbors!(counts, dv, N)\n    i = (blockIdx().x - 1) * blockDim().x + threadIdx().x\n    i > N && return nothing\n    @inbounds counts[i] = Int32(0)\n    for_each_neighbor_device(dv, i) do j\n        if j != Int32(i)\n            @inbounds counts[i] += Int32(1)   # global-memory accumulator,\n                                              # not a captured scalar\n        end\n    end\n    return nothing\nend\n\ncounts[i] is a global-memory slot, so no boxing happens. Each thread writes to its own index, so there's no race.","title":"Closure form (read-only body)"},{"category":"section","location":"gpu/#Macro-form-(per-thread-cursor-/-accumulator)","page":"GPU (CUDA)","text":"If the body advances a per-thread cursor, use the macro:\n\nusing StaticArrays  # the macro uses MVector internally; no other deps\n\nfunction write_edges!(senders, receivers, rd, rdn, cursors, dv, coords, N, radius)\n    i = (blockIdx().x - 1) * blockDim().x + threadIdx().x\n    i > N && return nothing\n    i32 = Int32(i)\n\n    @inbounds oo = cursors[i]      # cursor lives in register from here on\n    inv_r = 1f0 / radius\n    px = @inbounds coords[1, i]\n    py = @inbounds coords[2, i]\n    pz = @inbounds coords[3, i]\n\n    @for_each_neighbor_device_inline dv i j begin\n        if j != i32\n            qx = @inbounds coords[1, j]\n            qy = @inbounds coords[2, j]\n            qz = @inbounds coords[3, j]\n            dx = qx - px; dy = qy - py; dz = qz - pz\n            d2 = dx*dx + dy*dy + dz*dz\n            @inbounds senders[oo]   = j\n            @inbounds receivers[oo] = i32\n            @inbounds rd[1, oo] = dx * inv_r\n            @inbounds rd[2, oo] = dy * inv_r\n            @inbounds rd[3, oo] = dz * inv_r\n            @inbounds rdn[oo] = sqrt(d2) * inv_r\n            oo += Int32(1)            # mutates a local register, not boxed\n        end\n    end\n\n    @inbounds cursors[i] = oo\n    return nothing\nend\n\nThe macro provides:\n\nj (the neighbor index, Int32) bound for the body — pass the symbol you want to use as the third argument.\nPre-checked distance: body only fires when ‖coords[i] - coords[j]‖² ≤ radius².\nA per-thread MVector{40, Int32} traversal stack (sufficient for trees up to ~40 levels deep, which covers 2³² particles in 3D).","title":"Macro form (per-thread cursor / accumulator)"},{"category":"section","location":"gpu/#Launching-the-kernel","page":"GPU (CUDA)","text":"device_view(tns, qid, tid) returns an isbits struct safe to pass into a @cuda kernel:\n\ndv = device_view(tns, id, id)\ncounts = CUDA.zeros(Int32, N)\n@cuda threads = 128 blocks = cld(N, 128) count_neighbors!(counts, dv, Int32(N))\nCUDA.synchronize()","title":"Launching the kernel"},{"category":"section","location":"gpu/#Scope-and-limits","page":"GPU (CUDA)","text":"2D and 3D are both native. Construct TNS(Float32; ndims=2) for a quadtree; the GPU path uses a 4-way fan-out and the @for_each_neighbor_device_inline_2d macro for write-pass kernels. Coords are (NDIMS, N) matrices on either device — no padding.\nThe macro requires StaticArrays to be loadable; that dependency is already in Octopus's Project.toml, so calling code only needs using Octopus, CUDA (and optionally using StaticArrays if you also use MVector directly in your own code).\nStack depth is fixed at 40. If your tree is deeper (e.g. very-extreme leaf size ratios), increase the constant in src/macros.jl and rebuild — open an issue if you hit this in practice.\nfor_each_neighbor_device is the public function — keep it for read-only bodies. The macro is the recommended path for write-pass kernels.","title":"Scope and limits"},{"category":"section","location":"gpu/#Performance-recipe","page":"GPU (CUDA)","text":"For a write-pass kernel modelled after point_neighbor_ns:\n\nPer-point cursor in a register: hold oo = cursors[i] once, advance in-register inside the macro body, write back at the very end.\nPrecompute inv_r = 1f0 / radius: replace dx / radius with dx * inv_r in the body. On Float32 the compiler folds 4–6 divisions per edge into multiplies.\nMatch coords shape to the body's actual dimensions: when 2D-padded coords are passed to Octopus, your kernel can still compute features in the original 2D space (the macro filters by 3D distance, which equals 2D distance when z = 0).\nOne thread per query point: the simplest, most predictable mapping. Warp-cooperative descent is a v0.2 optimization.\n\nFollowing this recipe gives you the macro-form numbers above on any modern NVIDIA GPU.\n\n\n\nCopyright (c) 2026 Josef Jouaux, Chair of Mechatronics, University of Augsburg. Released under the MIT License.","title":"Performance recipe"},{"category":"section","location":"gnn/#Differentiable-edges","page":"Differentiable edges","text":"Neighbor search is a discrete operation, but the features it produces — relative displacements and distances — are smooth functions of the point coordinates. build_edges_diff exposes that smooth part to a reverse-mode AD system, so a radius graph can sit inside a learned model and gradients still reach the coordinates that produced it.","title":"Differentiable edges"},{"category":"section","location":"gnn/#Loading-the-extension","page":"Differentiable edges","text":"The rules live in weak extensions, so nothing is loaded until you ask for it:\n\nusing Octopus\nusing ChainRulesCore    # activates OctopusChainRulesCoreExt (CPU)\nusing Zygote            # or any other ChainRules-compatible AD\n\nFor the GPU path, add using CUDA as well — that activates OctopusCUDAChainRulesCoreExt, a separate hand-written kernel that accumulates the pullback with atomic adds.","title":"Loading the extension"},{"category":"section","location":"gnn/#Usage","page":"Differentiable edges","text":"using Octopus, ChainRulesCore, Zygote\n\nradius = 0.25f0\n\nfunction loss(coords)\n    tns = TNS(Float32)\n    set_search_radius!(tns, radius)\n    id = add_point_set!(tns, coords)\n    set_active_search!(tns, id, id)\n    run!(tns)\n\n    e = build_edges_diff(coords, tns, id, radius)\n    return sum(abs2, e.rel_displacement) + 0.5f0 * sum(abs2, e.rel_dist_norm)\nend\n\ncoords = rand(Float32, 3, 1_000)\ng = Zygote.gradient(loss, coords)[1]    # (3, 1000), same shape as coords\n\nThe return value has the same four fields as build_edges — senders, receivers, rel_displacement, rel_dist_norm — but the arrays are fresh copies rather than views onto the reused EdgeBuffer. That matters: without the copy, a later build_edges call would overwrite the values the pullback closed over.","title":"Usage"},{"category":"section","location":"gnn/#What-is-and-is-not-differentiable","page":"Differentiable edges","text":"Gradients flow to coords through rel_displacement and rel_dist_norm only. The tree topology, the edge indices and the search radius are treated as constants:\n\nsenders and receivers are integer indices with no continuous gradient. A loss that depends only on them (e.g. the edge count) yields a zero or nothing gradient — which is correct, not a bug.\nThe set of edges is held fixed at its value for the current coordinates. An infinitesimal coordinate perturbation that pushes a pair across the radius boundary is not modeled; the gradient is the derivative of the features of the current edge set.\n\nThe setup functions (run!, add_point_set!, set_search_radius!, set_active_search! and friends) are declared @non_differentiable, so you can call them inside a differentiated function without wrapping each one in Zygote.@ignore.","title":"What is and is not differentiable"},{"category":"section","location":"gnn/#Keeping-coords-and-tns-in-sync","page":"Differentiable edges","text":"build_edges_diff takes coords as an explicit first argument because that is the tensor the AD system tracks — but the edges come from whatever geometry tns was last built against. Keeping the two consistent is the caller's job. The simplest correct pattern is to rebuild inside the differentiated function, as in the example above. In a training loop that reuses one handle:\n\nfunction loss(coords)\n    Zygote.@ignore begin\n        update_point_set!(tns, id, coords)\n        run!(tns)\n    end\n    e = build_edges_diff(coords, tns, id, radius)\n    return my_model(e)\nend\n\nPass the same radius you gave set_search_radius! — it is the normalization constant for both feature arrays, and a mismatch silently rescales the gradient rather than erroring.","title":"Keeping coords and tns in sync"},{"category":"section","location":"gnn/#Verification","page":"Differentiable edges","text":"The CPU rules are checked against a 4-point central-difference reference in 2D and 3D, to rtol = 1e-4, for displacement-only, distance-only and combined losses. The GPU rules are checked element-wise against the CPU rules, which pins the sign convention on the displacement term. Those tests live in test/chainrules.jl and run in CI; the GPU half additionally requires JULIA_OCTOPUS_TEST_CUDA=1 and a working device.\n\nTo run them locally:\n\n$ JULIA_OCTOPUS_TEST_CHAINRULES=1 julia --project=. -e 'using Pkg; Pkg.test()'\n\n\n\nCopyright (c) 2026 Josef Jouaux, Chair of Mechatronics, University of Augsburg. Released under the MIT License.","title":"Verification"},{"category":"section","location":"#Octopus.jl","page":"Home","text":"Fast octree neighborhood search for Julia — on CPU and NVIDIA GPUs.\n\nEight octants, eight arms. An octree carves space into 8 children at every level; an octopus reaches out with 8 arms to grab everything within reach. That is exactly what this package does: for every point it grabs all neighbors inside a fixed radius, fast, with zero allocations on the hot path.\n\nOctopus.jl is a Julia port of the fast octree neighborhood search for SPH simulations (Fernández-Fernández et al., SIGGRAPH Asia 2022, DOI 10.1145/3550454.3555523). It mirrors the C++ TreeNSearch reference API and adds Julia-idiomatic and GPU-friendly conveniences on top, including differentiable edge construction for graph neural networks.","title":"Octopus.jl 🐙"},{"category":"section","location":"#Why-Octopus.jl?","page":"Home","text":"Fast. A cache-friendly Morton/octree build with an almost-sorted refit path for time-stepping simulations where points barely move between steps.\nAllocation-free hot loop. Iterate neighbors through a callback rather than materializing neighbor lists — no garbage on the critical path.\nCPU and GPU from one API. The same calls run multithreaded on the CPU (Polyester + SIMD) or on NVIDIA GPUs via a CUDA weak extension, where the tree build is fully device-resident.\nGNN-ready. Build flat-COO edge buffers and differentiate through them (Zygote / ChainRules), so neighbor graphs can sit inside a learned model.","title":"Why Octopus.jl?"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"Octopus\")\n\nOctopus.jl requires Julia 1.12 or newer. The GPU path additionally needs CUDA.jl, and the differentiable path needs ChainRulesCore.jl — both are weak dependencies, loaded on demand:\n\nPkg.add(\"CUDA\")             # optional: NVIDIA GPU backend\nPkg.add(\"ChainRulesCore\")   # optional: gradients through build_edges_diff","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using Octopus\n\nxyz = rand(Float32, 3, 100_000)\n\ntns = TNS(Float32; ndims = 3)\nset_search_radius!(tns, 0.02f0)\nid = add_point_set!(tns, xyz)\nset_active_search!(tns, id, id)\nrun!(tns)\n\nfor_each_neighbor(tns, id, id, 1) do j\n    # use neighbor j of point 1\nend","title":"Quick start"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"The Guide walks through the full workflow: point sets, active pairs, iteration, materialized lists, refit mode and z-sorting.\nGPU (CUDA) covers the device-resident build and in-kernel neighbor iteration.\nDifferentiable edges shows how to backpropagate through the neighbor graph.\nThe API reference documents every exported symbol.","title":"Where to go next"},{"category":"section","location":"#Scope","page":"Home","text":"2D and 3D, fixed radius. Pass ndims = 2 to TNS for a quadtree; the default ndims = 3 gives an octree. Coordinates are (NDIMS, N) matrices.\nTNS{Float32} is the tested and benchmarked path. TNS{Float64} works but is unbenchmarked.\nCPU and CUDA (NVIDIA). AMDGPU and Metal require kernel rewrites and are out of scope.\nRefit-mode temporal coherence for time-stepping simulations.","title":"Scope"},{"category":"section","location":"#Citation","page":"Home","text":"If you use this package, please cite the original paper:\n\n@article{FernandezFernandez2022,\n  author = {Fern{\\'{a}}ndez-Fern{\\'{a}}ndez, Jos{\\'{e}} Antonio and Westhofen, Lukas and L{\\\"{o}}schner, Fabian and Jeske, Stefan Rhys and Longva, Andreas and Bender, Jan},\n  title = {Fast Octree Neighborhood Search for SPH Simulations},\n  journal = {ACM Transactions on Graphics},\n  volume = {41},\n  number = {6},\n  year = {2022},\n  doi = {10.1145/3550454.3555523},\n}","title":"Citation"},{"category":"section","location":"#License","page":"Home","text":"Copyright (c) 2026 Josef Jouaux, Chair of Mechatronics, University of Augsburg. Released under the MIT License.","title":"License"}]
}
